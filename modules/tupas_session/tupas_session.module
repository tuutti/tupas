<?php
/**
 * @file
 * Contains TUPAS session module.
 */

/**
 * Implements hook_user_logout().
 */
function tupas_session_user_logout($account) {
  // Destroy tupas session on logout.
  $manager = \Drupal::service('tupas_session.session_manager');
  $manager->destroy($account->id());
}